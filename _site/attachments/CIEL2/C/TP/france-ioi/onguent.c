#include <stdio.h>

int main() {
    int deplacements[10000] = {0};
    int nbDeplacements = 0;
    scanf("%d", &nbDeplacements);

    for (int i = 0; i < nbDeplacements; i++)
    {
        scanf("%d", deplacements[0]);
    }

    return 0;
}